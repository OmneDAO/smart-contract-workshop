import { Transaction, TransactionPriority } from './types';
import { RpcProvider } from './provider';
import { Signer } from './signer';
export interface TxBuildOptions {
    from: string;
    to: string;
    valueOMC?: string | number;
    data?: string;
    priority?: TransactionPriority;
    gasLimit?: number;
    gasPriceQuar?: string;
    nonce?: number;
    layer?: 'commerce' | 'security';
}
export declare class TxBuilder {
    private provider;
    constructor(provider: RpcProvider);
    build(txOpts: TxBuildOptions): Promise<Transaction>;
    signAndSend(builderOpts: TxBuildOptions, signer: Signer): Promise<import("./types").TransactionReceipt>;
}
//# sourceMappingURL=tx-builder.d.ts.map