/**
 * Utility functions for Omne SDK
 *
 * Provides quar-precision arithmetic, address validation,
 * and other helper functions for Omne blockchain integration.
 */
import Big from 'big.js';
/**
 * Quar conversion constants
 */
export declare const QUAR_PER_OMC: Big.Big;
export declare const QUAR_PER_MICRO_OMC: Big.Big;
export declare const QUAR_PER_MILLI_OMC: Big.Big;
/**
 * Convert OMC amount to quar (10^-18 OMC precision)
 */
export declare function toQuar(omcAmount: string | number | Big): string;
/**
 * Convert quar amount to OMC
 */
export declare function fromQuar(quarAmount: string | number | Big): Big;
/**
 * Convert quar amount to OMC (alias for fromQuar)
 */
export declare function toOMC(quarAmount: string | number | Big): Big;
/**
 * Convert OMC amount to quar (alias for toQuar)
 */
export declare function fromOMC(omcAmount: string | number | Big): string;
/**
 * Format quar amount as human-readable OMC string with specified decimals
 */
export declare function formatBalance(quarAmount: string | number | Big, decimals?: number): string;
/**
 * Encode 20-byte address as bech32m om1z format.
 *
 * Format: om1z<data><checksum>
 *   - HRP "om" + separator "1" + witness version 2 ('z') + bech32m data
 *   - 20-byte payload → ~42 character address
 */
export declare function toOmneAddress(addressBytes: Uint8Array): string;
/**
 * Decode an om1z bech32m address to its 20-byte payload.
 */
export declare function fromOmneAddress(address: string): Uint8Array;
/**
 * Parse address string to bytes. Accepts om1z bech32m and raw 40-char hex.
 */
export declare function parseAddress(address: string): {
    format: 'bech32m' | 'hex';
    bytes: Uint8Array;
};
/**
 * Validate address format (om1z bech32m, legacy omne1, or raw hex)
 */
export declare function isValidAddress(address: string): boolean;
/**
 * Validate om1z bech32m address format specifically.
 */
export declare function isValidOmneAddress(address: string): boolean;
/**
 * Validate raw hex address format (40 lowercase hex chars, no prefix).
 */
export declare function isValidHexAddress(address: string): boolean;
/**
 * Normalize any supported address format to canonical om1z bech32m.
 */
export declare function normalizeAddress(address: string): string;
/**
 * Verify an ed25519 signature against a message and expected address.
 *
 * Ed25519 does not support public key recovery from a signature alone;
 * the caller supplies the signer's public key so we can verify the
 * signature and confirm the public key maps to the expected om1z address.
 */
export declare function verifyEd25519Signature(message: string, signatureHex: string, publicKeyHex: string, expectedAddress: string): boolean;
/**
 * Verify a signed message against an expected address.
 *
 * @deprecated Use verifyEd25519Signature() which takes a public key.
 * This wrapper exists for backward compatibility but requires both
 * signature and public key concatenated (64-byte sig + 32-byte pubkey = 96 bytes).
 */
export declare function verifyMessageSignature(message: string, signature: string, expectedAddress: string): boolean;
/**
 * Calculate gas cost in quar
 */
export declare function calculateGasCost(gasUsed: number, gasPriceQuar: string | number | Big): string;
/**
 * Estimate gas for different transaction types
 */
export declare function estimateGas(transactionType: 'transfer' | 'tokenTransfer' | 'contractDeploy' | 'orc20Deploy', hasData?: boolean): number;
/**
 * Generate random hex string
 */
export declare function randomHex(bytes: number): string;
/**
 * Generate Omne block hash with bh_ prefix
 */
export declare function generateBlockHash(): string;
/**
 * Generate Omne transaction hash with canonical txn_ prefix
 */
export declare function generateTransactionHash(): string;
/**
 * Validate Omne block hash format
 */
export declare function isValidBlockHash(hash: string): boolean;
/**
 * Validate Omne transaction hash format
 */
export declare function isValidTransactionHash(hash: string): boolean;
/**
 * Sleep utility for async operations
 */
export declare function sleep(ms: number): Promise<void>;
/**
 * Retry utility for network operations
 */
export declare function retry<T>(operation: () => Promise<T>, maxRetries?: number, delayMs?: number): Promise<T>;
/**
 * Validate transaction parameters
 */
export declare function validateTransaction(tx: {
    from: string;
    to: string;
    value: string;
    gasLimit: number;
    gasPrice: string;
    nonce: number;
}): void;
/**
 * Parse URL for WebSocket/HTTP detection
 */
export declare function parseRpcUrl(url: string): {
    protocol: 'ws' | 'wss' | 'http' | 'https';
    host: string;
    port?: number;
    path: string;
};
/**
 * Check if running in browser environment
 */
export declare function isBrowser(): boolean;
/**
 * Check if running in Node.js environment
 */
export declare function isNode(): boolean;
/**
 * Safe integer conversion
 */
export declare function safeInt(value: string | number | undefined, defaultValue?: number): number;
/**
 * Safe string conversion
 */
export declare function safeString(value: any, defaultValue?: string): string;
/**
 * Convert hex string to buffer.
 *
 * Expects raw lowercase hex with no prefix.  This is the Omne convention.
 */
export declare function hexToBuffer(hex: string): Uint8Array;
/**
 * Convert buffer to hex string.
 *
 * Omne convention: raw lowercase hex with no prefix.  The Omne ecosystem
 * does not use the Ethereum "0x" prefix; addresses use "omne1" instead.
 */
export declare function bufferToHex(buffer: Uint8Array): string;
/**
 * Generate deterministic ID for requests
 */
export declare function generateRequestId(): string;
//# sourceMappingURL=utils.d.ts.map