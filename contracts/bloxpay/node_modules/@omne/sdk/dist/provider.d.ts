import { OmneClient } from './client';
import { Transaction, TransactionReceipt } from './types';
export interface RpcProviderOptions {
    url: string;
    timeoutMs?: number;
    retries?: number;
}
export declare class RpcProvider {
    private client;
    constructor(options: RpcProviderOptions);
    connect(): Promise<void>;
    disconnect(): Promise<void>;
    getNetworkInfo(): Promise<import("./types").NetworkInfo>;
    sendTransaction(tx: Transaction): Promise<TransactionReceipt>;
    callRpc<T = any>(method: string, params?: any[]): Promise<T>;
    getClient(): OmneClient;
}
//# sourceMappingURL=provider.d.ts.map