/**
 * Secure cryptographic utilities for Omne TypeScript SDK
 *
 * AES implementation uses aes-js in Node/Jest and WebCrypto as fallback.
 * Keeps KDF and MAC semantics compatible with existing keystore tests:
 *  - cipher name: "aes-256-ctr"
 *  - MAC computed as keccak( encrypted || iv || salt || keccak(derivedKey || "mac") )
 *
 * NOTE: This file is a corrected replacement for the earlier secure-crypto.ts
 * that avoided importing '@noble/ciphers/aes' to prevent Jest ESM resolution issues.
 */
export interface SecureKeyDerivationOptions {
    algorithm?: 'pbkdf2' | 'scrypt';
    iterations?: number;
    saltLength?: number;
    keyLength?: number;
    scryptOptions?: {
        N: number;
        r: number;
        p: number;
    };
}
export interface SecureEncryptionResult {
    ciphertext: string;
    salt: string;
    iv: string;
    algorithm: string;
    kdf: string;
    kdfParams: any;
    mac: string;
}
export declare function deriveKey(password: string, salt: Uint8Array, options?: SecureKeyDerivationOptions): Promise<Uint8Array>;
export declare function secureEncrypt(data: string | Uint8Array, password: string, options?: SecureKeyDerivationOptions): Promise<SecureEncryptionResult>;
export declare function secureDecrypt(encryptionResult: SecureEncryptionResult, password: string): Promise<Uint8Array>;
export declare function generateSecureId(): string;
export declare function generateSecureRandom(bytes?: number): string;
export declare function secureZero(buffer: Uint8Array): void;
export declare function secureRandomBytes(size: number): Uint8Array;
//# sourceMappingURL=secure-crypto.d.ts.map